import 'package:flutter/material.dart';
class PreviewUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("123"),
      )
    );
  }
}
